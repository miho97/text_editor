<Window x:Class="TextEditorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonEdit="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
        xmlns:local="clr-namespace:TextEditorApp"
        mc:Ignorable="d"
        Title="TextEditor" Height="450" Width="800">
    <DockPanel Margin="1">
        <Ribbon DockPanel.Dock="Top" Height="48">
            <!-- Application Menu -->
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu KeyTip="F">
                    <RibbonApplicationMenuItem Header="New" Click="NewFile_Click"/>
                    <RibbonApplicationMenuItem Header="Open"/>
                    <RibbonApplicationMenuItem Header="Save"/>
                    <RibbonApplicationMenuItem Header="Save As..."/>
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>

            <!-- Format Options -->
            <RibbonMenuButton Label="Format">
                <RibbonMenuItem Header="Font"/>
                <RibbonMenuItem Header="Paragraph"/>
                <RibbonMenuItem Header="Color"/>
                <!-- More format options -->
            </RibbonMenuButton>

            <!-- Settings -->
            <RibbonMenuButton Label="Settings">
                <RibbonMenuItem Header="Edit Preferences"/>
                <RibbonMenuItem Header="Theme Settings"/>
                <RibbonMenuItem Header="Language"/>
                <!-- More settings options -->
            </RibbonMenuButton>

            <!-- C# Highlighting -->
            <RibbonMenuButton Label="C# sharp Highlighting">
                <RibbonMenuItem Header="Enable Highlighting" Click="EnableHighlighting_click"/>
                <RibbonMenuItem Header="Disable Highlighting"/>
                <!-- Additional options for C# highlighting -->
            </RibbonMenuButton>

            <!-- Find & Replace -->
            <RibbonMenuButton Label="Find Replace">
                <RibbonMenuItem Header="Find"/>
                <RibbonMenuItem Header="Replace"/>
                <!-- Additional options for Find & Replace -->
            </RibbonMenuButton>
        </Ribbon>

        <!-- Main Content Area -->
        <DockPanel>
            <TabControl x:Name="MainTabControl">
                <TabItem Header="File 1" PreviewMouseRightButtonDown="TabItem_PreviewMouseRightButtonDown">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Bottom">Status bar for File 1</TextBlock>
                        <avalonEdit:TextEditor Name="textEditor" VerticalScrollBarVisibility="Visible"
                                               ShowLineNumbers="True">
                        </avalonEdit:TextEditor>
                    </DockPanel>
                </TabItem>
                <!-- Additional TabItems can be added here -->
            </TabControl>
        </DockPanel>
    </DockPanel>
</Window>